# --- Núcleo API ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6,<2.10
pydantic-settings>=2.2,<2.6
eval-type-backport>=0.2   # habilita X|None en Python 3.9 (requerido por pydantic)
python-multipart>=0.0.9      # uploads (multipart)

# --- Ingesta / OCR (reusa Due-Legal) ---
PyMuPDF>=1.23
pytesseract>=0.3             # requiere binario Tesseract + idioma 'spa'

# --- LLM (redacción y extracción blanda) ---
anthropic>=0.40
openai>=1.40          # fallback OpenAI-compatible (Infermatic / TotalGPT)
python-dotenv>=1.0

# --- Joya: transcripción de descargos ---
openai-whisper>=20231117     # o faster-whisper para velocidad local

# --- J1: telefonía de descargos (ElevenLabs Agents + Twilio WhatsApp) ---
requests>=2.31               # llamadas HTTP a ElevenLabs / Twilio

# --- Base de datos (storage relacional) ---
SQLAlchemy>=2.0          # ORM; SQLite en demo, Postgres en prod (misma capa)

# --- Exportación / Parseo Excel ---
openpyxl>=3.1               # lectura .xlsx + generación plantilla liquidación

# --- Tests (la prueba de confiabilidad) ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27                  # TestClient async
